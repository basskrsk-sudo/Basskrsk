# ТАЙГА — Инструкция по деплою на Amvera (сервер на Node.js)

## Что нового в v90

- В разделе «Заказы» супер-администратор может назначить или исправить
  грумера у уже оплаченного заказа кнопкой «Грумер».
- В списке доступны только активные грумеры той же точки; сразу показана
  будущая комиссия по заказу.
- Для финансового исправления обязательна причина. Старое и новое назначение,
  ставки, заказ и администратор сохраняются в «Журнале действий».
- Заказ нельзя переназначить после того, как он вошёл в зафиксированную
  выплату грумеру, а также после возврата или при доставке.
- В быстром QR-оформлении больше нельзя потерять атрибуцию: если у точки есть
  грумеры, покупатель обязательно выбирает одного из них или «Выбрал всё сам».
- Явный выбор «Выбрал всё сам» сохраняется в заказе как комиссия 0% и виден в
  админке отдельно от старых заказов, в которых грумер просто не был записан.

## Что нового в v89

- Полная воронка Яндекс Метрики работает и в интернет-каталоге, и в быстром
  сценарии по QR товара.
- Фиксируются выбор минимаркета, открытие товара, «Купить сейчас», корзина,
  начало оформления, корректный телефон и переход к оплате.
- Отдельно измеряются создание платежа, успешная оплата, отмена платежа и
  техническая ошибка открытия платёжной формы.
- Подключение уведомлений после покупки фиксируется с каналом `max` или
  `telegram`.
- Повторный заказ измеряется отдельно при добавлении товаров из истории и
  после успешной оплаты.
- Параметр `scenario` разделяет обычный каталог (`catalog`) и быстрый QR
  (`qr`); в цели не передаются телефоны, email и другие персональные данные.
- Одноразовые платёжные цели защищены от дублей, если подтверждение пришло
  одновременно от виджета и от серверной проверки.

Цели типа «JavaScript-событие», которые нужно создать в Яндекс Метрике:

| Идентификатор цели | Этап |
| --- | --- |
| `minimarket_selected` | выбран или автоматически определён минимаркет |
| `product_opened` | открыт товар |
| `buy_now_clicked` | нажата кнопка быстрой покупки |
| `add_to_cart` | товар добавлен в корзину |
| `checkout_started` | начато оформление |
| `phone_entered` | введён корректный телефон |
| `checkout_to_payment` | клиент перешёл к подтверждению суммы |
| `payment_created` | заказ и платёж созданы сервером |
| `payment_success` | сервер подтвердил успешную оплату |
| `payment_cancelled` | платёж отменён или завершился неуспешно |
| `payment_create_error` | не удалось создать или открыть платёжную форму |
| `notifications_connected` | подключён MAX или Telegram |
| `repeat_order_started` | товары добавлены из прошлого заказа |
| `repeat_order_completed` | повторный заказ успешно оплачен |
| `purchase` | совместимая итоговая цель покупки с суммой заказа |

Главная конверсия воронки: `checkout_started` → `payment_success`.

## Что нового в v88

- Верхнее клиентское меню сокращено до пяти основных направлений: каталог,
  точки покупки, инструкция, бонусы и личный кабинет.
- Ссылки «Партнёрам», «Инвесторам», игры и служебные входы оставлены в
  подвале и больше не отвлекают от покупки в основном меню.
- Нижняя мобильная навигация приведена к той же структуре; вместо дублирующей
  «Главной» добавлен быстрый переход к инструкции покупки.
- Страница «Как это работает» получила единое клиентское меню.
- Основные разделы теперь имеют прямые адреса `?page=catalog`,
  `?page=contacts`, `?page=bones` и `?page=account`.

## Что нового в v87

- В карточках товаров показано наличие именно в выбранном минимаркете и
  предупреждение «Осталось N», когда доступно не больше двух единиц.
- Все доступные фасовки и их цены видны прямо в каталоге, без открытия
  страницы товара.
- Добавлены сведения «Для кого подходит», «Назначение», состав и ограничения;
  для существующих товаров используются безопасные подсказки по категории.
- У карточки теперь два явных действия: «Купить сейчас» и «В корзину».
- На странице товара остаток обновляется вместе с выбранной фасовкой, а
  количество нельзя увеличить выше доступного остатка.
- В админке появились отдельные поля для описания, назначения, подходящих
  питомцев, состава и ограничений.

## Что нового в v86

- Экран после оплаты показывает купленные товары с фото, названием, фасовкой
  и количеством.
- Для самовывоза крупно выводится «Возьмите товар с полки», для доставки —
  сообщение об ожидании звонка для согласования времени.
- Адрес минимаркета, номер заказа и итоговая сумма показаны до следующих
  действий клиента.
- Добавлены кнопка «Открыть мой заказ» и возврат «Купить ещё» в каталог того
  же минимаркета.
- После покупки можно подключить уведомления через MAX или Telegram; MAX
  отображается первым, если интеграция настроена.
- Те же кнопки заказа и уведомлений добавлены в быстрый сценарий по QR товара.
- Полный экран результата восстанавливается и после возврата с внешней
  страницы ЮKassa.

## Что нового в v85

- На входе в личный кабинет телефон вводится один раз для всех способов.
- MAX расположен первым, Telegram — вторым; оба действия показаны крупными
  мобильными кнопками.
- Последний сохранённый телефон автоматически подставляется при открытии.
- Пароль, восстановление и регистрация скрыты под ссылкой «Войти другим
  способом» и больше не перегружают основной экран.

## Что нового в v84

- Первый экран главной страницы сосредоточен на одном обещании: выбрать
  ближайший минимаркет, оплатить онлайн и сразу забрать товар.
- Основное действие — «Найти ближайший минимаркет», вторичное —
  «Смотреть все товары».
- Категории, случайные товары и четыре преимущества перенесены ниже первого
  экрана, чтобы они не конкурировали с главным действием.
- Мобильный первый экран стал короче: заголовок, пояснение и обе кнопки
  помещаются до блоков каталога и преимуществ.

## Что нового в v83

- Выбор в поле «Кто посоветовал товар?» снова обязателен.
- Добавлен отдельный вариант «Выбрал всё сам» — он позволяет продолжить
  оформление без выбора грумера и не начисляет кому-либо комиссию.
- Персональная ссылка партнёра по-прежнему автоматически выбирает нужного
  грумера, если он работает в указанном минимаркете.

## Что нового в v82

- Поле «Кто посоветовал товар?» больше не обязательно и содержит вариант
  «Никто / не выбираю»; отсутствие выбора не мешает перейти к оплате.
- Грумер подставляется автоматически только из проверенной персональной ссылки
  `?ref=POINT_ID&partner=PARTNER_ID`, если он действительно работает на этой точке.
- В кабинете партнёра появилась готовая персональная ссылка для покупателей.
- Ранее выбранный минимаркет и последний телефон автоматически подставляются в
  обычное оформление; доставка остаётся только в интернет-каталоге.
- На телефоне кнопка оплаты закреплена внизу прокручиваемого окна и постоянно
  показывает итоговую сумму.
- При нескольких партнёрах сервер больше не назначает ставку комиссии первому
  сотруднику случайно, если покупатель никого не выбрал.

## Что нового в v81

- Товарный QR сразу фиксирует товар и минимаркет: повторного выбора точки,
  грумера или способа получения нет.
- Количество в быстрой покупке фиксировано как `1`; клиенту остаётся ввести
  или подтвердить сохранённый телефон и перейти к оплате.
- Последний телефон подставляется автоматически и используется также в
  обычном оформлении заказа.
- Точка из QR запоминается для кнопки «Купить ещё товар» и будущих посещений
  каталога.
- После оплаты крупно показано «Оплачено» и «Возьмите товар с полки», а также
  товар, фасовка, название и адрес минимаркета.
- Резервный возврат с внешней страницы ЮKassa теперь возвращает на тот же
  товарный QR-маршрут и восстанавливает экран успешной покупки.

## Что нового в v80

- Главная кнопка теперь прямо ведёт к выбору ближайшего минимаркета.
- До выбора точки каталог и товары не показываются; после выбора открывается
  только фактически доступный в этой точке ассортимент.
- Выбранная точка запоминается отдельно для каждого города.
- Сверху каталога закреплена постоянная плашка с названием и адресом
  минимаркета и кнопкой «Изменить».
- Убран дублирующий выпадающий список точки внутри уже открытого каталога.
- При смене точки корзина с товарами другой точки по-прежнему безопасно
  очищается только после подтверждения клиента.

## Что нового в v72

- В заголовке партнёрского раздела убрана формулировка «с ваших клиентов».
- Новый заголовок: «Дополнительный доход — без вложений в товар».

## Что нового в v71

- Карточки опубликованных салонов в разделе «Партнёрам» теперь целиком
  кликабельны и открывают подробную публичную страницу точки.
- На карточке появилась заметная кнопка «Подробнее и все фото» и счётчик
  фотографий; кнопка каталога по-прежнему открывает товары выбранной точки.
- На странице салона доступны описание, адрес, внешние ссылки и вся галерея
  с крупным просмотром, стрелками и миниатюрами.
- Для каждой опубликованной страницы появился постоянный адрес вида
  `/?page=salon&salon=ID`, который можно отправить клиенту и открыть повторно.

## Что нового в v70

- Исправлена загрузка фотографий салонов в сборках Amvera, где ImageMagick
  установлен без рабочего модуля JPEG/WebP.
- Фото теперь уменьшается до 1600×1200 прямо в браузере перед отправкой —
  загрузка с телефона стала быстрее, а постоянное хранилище расходуется меньше.
- Сервер проверяет реальную сигнатуру JPG, PNG и WebP и при сбое конвертера
  безопасно сохраняет исходный файл вместо показа ложной ошибки формата.
- Обновлён кэш кабинетов, чтобы исправление применилось без ручной очистки
  данных сайта в браузере.

## Что нового в v69

- Из вкладки «Партнёрам» удалён длинный технический блок об обслуживании
  стойки владельцем точки.
- Блок о рыночном тренде заменён на понятное предложение выгоды: 5% владельцу
  за размещение и 15–20% грумеру за участие в продажах.
- Форма заявки сокращена до имени, фамилии и номера телефона.

## Что нового в v68

- Из публичной вкладки «Партнёрам» удалён набор менеджеров и все связанные
  с ним рекламные тексты, условия и кнопки.
- Вкладка теперь предназначена только для владельцев точек и грумеров.
- Калькулятор дохода переделан под две роли: 5% владельцу за размещение и
  15–20% грумеру за участие в продажах; при совмещении доход складывается.
- Служебный вход для уже работающих менеджеров сохранён в общем подвале сайта.

## Что нового в v67

- Добавлен отдельный реестр фактических выплат владельцам салонов.
- В админке сумма «К выплате» включает только оплаченные заказы, которые ещё
  не входили в предыдущие выплаты.
- Кнопка «Выплачено» фиксирует сумму и заказы одной транзакцией, обнуляет
  текущий счётчик и добавляет запись в историю.
- История выплат доступна администратору и самому владельцу салона.
- График выплат владельцам совпадает с графиком грумеров: 9-го и 24-го числа.

## Что нового в v66

- В разделе админки «Владельцы салонов» добавлена колонка «Накоплено» —
  вознаграждение владельца со всех оплаченных заказов закреплённой точки.
- Под общей суммой показаны начисление за текущий месяц и действующая
  процентная ставка владельца.
- В заголовке списка отображается общая накопленная сумма по всем владельцам.
- Расчёт выполняется по живым данным заказов и не требует ручного обновления.

## Что нового в v65

- Из обычного оформления заказа и быстрой покупки по товарному QR удалён
  лишний предварительный выбор между СБП и банковской картой.
- Теперь сайт открывает единую защищённую форму ЮKassa, а доступные способы
  оплаты клиент выбирает уже внутри неё.
- Из формы удалена фраза «Из персональных данных нужен только номер телефона».
- Старые открытые версии страницы, которые ещё передают конкретный способ
  оплаты, остаются совместимыми с сервером.

## Что нового в v64

- В разделе «Партнёрам» появился блок с карточками всех действующих точек
  сети — без акцента на их общем количестве.
- Карточка показывает фотографию-обложку салона, город, название, адрес и
  слоган; если фотографии нет, используется аккуратная фирменная заглушка.
- Из карточки можно открыть товары конкретной точки, а для опубликованных
  салонов — их подробную публичную страницу.
- Для блока добавлен отдельный публичный API, который не раскрывает внутренние
  данные менеджеров и условия партнёров.

## Что нового в v63

- Вместо ссылки на одну фотографию салона можно выбрать и загрузить до восьми
  файлов JPG, PNG или WebP сразу с телефона или компьютера.
- Галерею могут редактировать администратор, менеджер точки, грумер и владелец
  салона: фотографию можно удалить из списка или сделать обложкой.
- Файлы сжимаются в WebP и сохраняются в постоянной папке `/data/uploads`,
  поэтому не исчезают при новом деплое.
- На публичной странице салона добавлены крупный просмотр, стрелки и лента
  миниатюр; прежняя фотография по ссылке автоматически становится обложкой.

## Что нового в v62

- В генераторах товарных QR-кодов администратора и менеджера количество
  этикеток теперь можно установить равным `0`.
- Товары с нулевым количеством остаются в предпросмотре, но не попадают на
  печатный лист А4.
- Если для всех товаров указано `0`, печать не запускается и показывается
  понятное сообщение.

## Что нового в v61

- Из административной панели удалён генератор большого QR, привязанного к
  конкретной точке. Для всех точек используется единый QR на общую страницу
  с пятью шагами покупки.
- В админке сохранены только товарные QR-коды, которые сразу открывают быструю
  карточку конкретного товара выбранной точки.
- Сохранены массовая генерация, скачивание, печать и листы этикеток А4.

## Что нового в v60

- Информация о партнёрских салонах по-прежнему хранится в основной SQLite-базе
  `/data/taiga.db`, а теперь дополнительно автоматически сохраняется в
  `/data/salon-pages-backup.json` после каждого редактирования.
- При запуске нового деплоя приложение сравнивает данные со страховочной
  копией и восстанавливает пропавшие или более свежие карточки салонов.
- Более новые изменения в рабочей базе не перезаписываются старым снимком;
  удалённые точки также удаляются из страховочной копии.

## Что нового в v59

- Маскот убран с интерактивной области Яндекс.Карт и перенесён в отдельную
  информационную карточку под картой.
- Проведён аудит всех маскотов на главном сайте: сохранены пропорции,
  ограничены чрезмерные размеры и исключено растягивание изображений.
- На мобильных экранах маскот карточки товара больше не обтекает заголовок,
  а в корзине и окне оплаты для иллюстраций зарезервировано безопасное место.
- Уменьшен маскот страницы 404 и обновлена версия service worker, чтобы
  браузеры не сохраняли старую раскладку.

## Что нового в v58

- В верхнюю навигацию добавлена заметная вкладка «Партнёрам».
- Первый экран партнёрского раздела показывает выгоду владельца салона:
  5% за размещение, 0 ₽ вложений и отсутствие собственной закупки, кассы,
  учёта и логистики.
- Отдельно показан доход грумера 15–20% и возможность совмещать две роли.
- Добавлены быстрые кнопки заявки и калькулятора дохода.
- Раздел получил прямую ссылку `/?page=partners` для отправки потенциальным
  партнёрам; ссылка корректно открывает нужную вкладку после загрузки сайта.

## Что нового в v57

- Блок «Популярно рядом» теперь использует количество реально оплаченных
  продаж, а не ручные плашки «Хит» и «Новинка».
- Полностью возвращённые заказы не учитываются в популярности.
- Товары с фотографиями имеют приоритет; при недостатке истории продаж блок
  дополняется случайными товарами с фото, а карточки без фото идут последними.

## Что нового в v56

- Исправлена мобильная нижняя навигация: она больше не растягивается на всю
  высоту экрана и не перекрывает страницу полупрозрачным слоем.
- Для нижнего меню явно сбрасывается унаследованное от верхней шапки правило
  `top: 0`; панель снова закреплена только у нижней границы экрана.

## Что нового в v55

- Закреплена отдельная мобильная страница `/purchase-guide.html` с быстрой
  покупкой за пять шагов: выбор товара, сканирование QR на упаковке, телефон,
  оплата через СБП/картой и самостоятельное получение.
- Проверено соответствие напечатанному QR-коду: он ведёт именно на
  `https://тайга-корм.рф/purchase-guide.html`.
- Добавлены canonical и Open Graph-метаданные страницы инструкции.

## Что нового в v54

- Новый быстрый маршрут `/p/{точка}/{товар}` для QR-кодов на упаковке: товар,
  фасовка, остаток, телефон и оплата без загрузки всей витрины.
- Старые QR вида `/?ref=...&product=...` автоматически перенаправляются на
  быстрый маршрут — перепечатывать уже размещённые этикетки не требуется.
- Генераторы QR администратора и менеджера сразу создают короткие ссылки `/p/...`.
- Если встроенный виджет ЮKassa не загрузился, сайт повторяет загрузку и затем
  открывает защищённую страницу оплаты ЮKassa. Заказ создаётся только после
  выбора рабочего сценария оплаты.
- Основной сайт, инструкция, быстрый QR-лендинг и служебные страницы приведены
  к единой палитре «хвоя + золото»: `#0A4A38` и `#E8A020`.
- Добавлены мобильные исправления для safe-area, полей ввода и быстрых тапов.
- В стартовом логе отображается фактическая версия Node.js.

## Что изменилось по сравнению со старой версией

Раньше сайт был чисто статическим (nginx + файлы), а данные (остатки, партнёры,
пароли) жили в `config.js` и `localStorage` браузера. Теперь это настоящее
серверное приложение на Node.js:

- Все данные — в базе SQLite на сервере (`/data/taiga.db`)
- Пароли партнёров/менеджеров/админа — хэшированы, не лежат открытым текстом
- Оплата — через ЮKassa (создание платежа и вебхук подтверждения — на сервере,
  с секретным ключом, которого никогда нет в браузере)
- Telegram-уведомления — тоже с сервера (надёжнее старой схемы из браузера)

**Внешних npm-зависимостей нет вообще** — сервер написан на встроенных
модулях Node.js (`node:http`, `node:sqlite`, `node:crypto`). Поэтому важно,
чтобы Amvera использовала **Node.js 22 или новее** — `node:sqlite` появился
в Node 22.5.

## Структура проекта

```
Dockerfile
package.json
server/              ← весь бэкенд
  server.js           точка входа
  db.js               схема и подключение к SQLite
  seed.js             начальные данные (точки, товары, админ)
  auth.js             хэширование паролей, токены
  http-utils.js       мини-роутер (без Express)
  routes-auth.js       /api/auth/login
  routes-products.js   /api/products, /api/points
  routes-partners.js   /api/partners/*
  routes-managers.js /api/managers/*
  routes-orders.js     /api/orders (просмотр)
  routes-payment.js    /api/create-payment, /api/yookassa-webhook
  reservations.js      резерв товара и косточек на время оплаты
  yookassa.js          клиент к API ЮKassa
  telegram.js          отправка уведомлений
public/               ← сайт (то же, что раньше отдавал nginx)
  index.html, taiga-admin.html, taiga-groomer.html, taiga-manager.html,
  taiga-register.html, taiga-qr.html, privacy-policy.html,
  config.js, favicon.ico, images/, qrcode.min.js, taiga_notion_template.xlsx
```

---

## ШАГ 1 — Переменные окружения (вместо части config.js)

Секретные данные теперь задаются переменными окружения на Amvera —
**не в файлах сайта**, чтобы они не попадали в браузер клиента.

В панели Amvera → раздел «Переменные окружения» добавьте:

```
YOOKASSA_SHOP_ID=ваш_shopId
YOOKASSA_SECRET_KEY=ваш_секретный_ключ
# Только для изолированного тестового стенда без реальных платежей:
# DEMO_MODE=true
# Необязательно: срок резерва 5–30 минут, по умолчанию 15:
# RESERVATION_TTL_MINUTES=15
TG_TOKEN=токен_бота_из_BotFather
TG_CHAT_ID=ваш_chat_id
# Текущий адрес бота без @. Меняйте только после смены username в BotFather:
TG_BOT_USERNAME=taiga_dog_bot
TOKEN_SECRET=любая_длинная_случайная_строка_для_подписи_токенов_входа
# MAX: токен и username одобренного бота, username без символа @
MAX_BOT_TOKEN=токен_бота_MAX
MAX_BOT_USERNAME=username_бота_MAX
# Случайная строка 32+ символа: A-Z, a-z, 0-9, _ и -
MAX_WEBHOOK_SECRET=отдельный_секрет_MAX_webhook
```

### MAX-бот «ХвостМаркет»

Сайт принимает события MAX по адресу:

`https://ваш-домен/api/max/webhook`

После деплоя и добавления трёх переменных зарегистрируйте Webhook один раз:

```bash
curl -X POST "https://platform-api2.max.ru/subscriptions" \
  -H "Authorization: ВАШ_MAX_BOT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://ваш-домен/api/max/webhook",
    "update_types": ["bot_started", "message_created"],
    "secret": "ВАШ_MAX_WEBHOOK_SECRET"
  }'
```

Ожидаемый ответ: `{"success":true}`. Токен и webhook-секрет нельзя
публиковать в коде, логах или переписке.

### Telegram-бот «ХвостМаркет»

При запуске сервер автоматически устанавливает отображаемое имя
«ХвостМаркет», фирменное описание и команды `/start` и `/help`. Тексты входа
и уведомлений также формируются под новым брендом.

В старых сборках токен Telegram по ошибке находился в публичном `config.js`.
Перед публикацией этой версии обязательно откройте `@BotFather`, перевыпустите
токен бота и замените значение `TG_TOKEN` в переменных Amvera. Старый токен
после перевыпуска перестанет работать.

Username бота не меняется кодом, потому что это его фактический адрес. Если
вы выберете новый свободный username в BotFather, после этого укажите его без
символа `@` в переменной `TG_BOT_USERNAME`, иначе ссылки входа будут вести по
старому адресу. Квадратную пиктограмму «лапка на геоточке» для аватара можно
взять из `public/images/icons/icon-512.png` и установить через BotFather.

**Как получить ключи ЮKassa:**
1. Зарегистрируйтесь на yookassa.ru как ИП
2. Личный кабинет → Настройки → API → скопируйте `shopId` и `Секретный ключ`
3. В личном кабинете ЮKassa укажите адрес для вебхуков:
   `https://ваш-домен/api/yookassa-webhook`

**COMPANY_INN / COMPANY_OGRNIP** и прочие несекретные настройки (название
бренда, город, реквизиты для договора с партнёрами) остаются в `public/config.js`,
как и раньше — это публичная информация, ей не место в переменных окружения.

### Почта (SMTP) — обязательно для восстановления пароля клиентов

Клиенты теперь входят либо через Telegram, либо по паролю (без SMS), а
забытый пароль восстанавливают по ссылке на почту. Без настроенной почты
восстановление молча не работает (сайт не покажет ошибку, письмо просто не
уйдёт) — обязательно добавьте:

```
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=ваш_ящик@yandex.ru
SMTP_PASSWORD=пароль_приложения_(не обычный пароль от почты)
```

Та же настройка уже используется для ежедневного бэкапа базы и уведомлений —
если она у вас уже была задана раньше, ничего дополнительно делать не нужно.

## ШАГ 2 — Персистентное хранилище (важно!)

База данных (SQLite) должна жить в папке `/data` — это специальная папка
Amvera, которая **не стирается** при пересборке приложения (в отличие от
папки с кодом, которая пересоздаётся заново при каждом деплое).

В конфигурации приложения на Amvera убедитесь, что:
- `persistenceMount` указывает на `/data` (это значение по умолчанию, менять
  не нужно, если не меняли раньше)
- Первый деплой обязательно должен пройти успешно один раз — после этого
  папка `/data` появится и данные будут сохраняться между обновлениями

Если вы вообще не настраивали `persistenceMount` — Amvera использует `/data`
по умолчанию, ничего дополнительно делать не нужно.

## ШАГ 2.5 — Резервная копия базы ПЕРЕД каждым обновлением (рекомендуется)

База данных живёт в одном файле `/data/taiga.db` — там всё сразу: заказы,
товары, клиенты, партнёры, косточки. Сама заливка новой версии код не
трогает, но перед любым обновлением стоит перестраховаться:

1. Зайдите в `taiga-admin.html`, раздел «Дашборд»
2. Найдите карточку «💾 Резервная копия базы данных»
3. Нажмите **«⬇️ Скачать бэкап сейчас»** — файл `taiga-backup-....db` скачается
   прямо на компьютер (это безопасный, консистентный снимок базы через
   встроенный механизм SQLite — можно смотреть его позже в любой
   программе для просмотра SQLite, например DB Browser for SQLite)
4. Только после этого заливайте новую версию

Рядом есть вторая кнопка «Отправить на почту сейчас» — то же самое, но
письмом (нужен настроенный `BACKUP_EMAIL`/`BUSINESS_COPY_EMAIL`); плюс
автоматическая копия и так уходит на почту раз в сутки без вашего участия.
Кнопка «Скачать» ни от чего не зависит и работает всегда.

Для восстановления супер-администратору доступна кнопка «Восстановить из
файла». Выберите ранее скачанный файл `.db` и подтвердите действие. Сервер
проверит, что это целая база «Тайги», автоматически сохранит текущую базу в
`/data/taiga-before-restore-....db`, затем перезапустится и установит выбранную
копию. Обычным администраторам эта кнопка не показывается.

Если автоматический перезапуск на Amvera отключён, после загрузки файла
перезапустите приложение вручную — подготовленная копия установится при старте.

## ШАГ 3 — Деплой

1. Загрузите на Amvera все файлы из этой папки (`Dockerfile`, `package.json`,
   `server/`, `public/`) — так же, как раньше загружали статику
2. Нажмите «Собрать и задеплоить»
3. Дождитесь статуса «Работает»
4. При первом запуске сервер сам создаст базу данных и заполнит её начальными
   данными (3 точки, 11 товаров, админ с логином `admin` / паролем `taiga2025#`)
5. **Сразу смените пароль администратора** — зайдите в `taiga-admin.html`,
   войдите со стандартными данными, потом либо через код, либо попросите
   помочь сменить пароль на сервере

## ШАГ 4 — Проверка

- Откройте `https://ваш-домен/` — каталог должен загрузиться (теперь из базы,
  не из захардкоженного списка)
- Откройте `https://ваш-домен/taiga-admin.html`, войдите как админ, проверьте
  разделы «Товары», «Заказы», «Точки», «Менеджеры»
- Сделайте тестовый заказ на сайте. Без `YOOKASSA_SHOP_ID` и
  `YOOKASSA_SECRET_KEY` оплата безопасно блокируется и заказ не создаётся.
- Для изолированного тестового стенда демо-оплату можно включить явно переменной
  `DEMO_MODE=true`. Никогда не задавайте её в боевой среде.
- Проверьте двумя параллельными попытками, что последняя единица товара и один
  баланс косточек доступны только одному заказу. Неоплаченный резерв снимается
  через 15 минут после безопасной отмены платежа в ЮKassa.

---

## Если что-то не работает

**«Cannot find module node:sqlite» или похожая ошибка**
→ Проверьте версию Node.js на Amvera — нужна 22.5 или новее. Если Amvera
предлагает более старую версию в конфигурации окружения, обновите её.

**Данные пропадают после каждого обновления сайта**
→ Значит `/data` не смонтирована как постоянное хранилище — проверьте
`persistenceMount` в конфигурации приложения (см. Шаг 2)

**Оплата не проходит / вебхук не приходит**
→ Проверьте, что в личном кабинете ЮKassa указан правильный адрес вебхука
(`https://ваш-домен/api/yookassa-webhook`) и что сайт работает по HTTPS

**Telegram-уведомления не приходят**
→ Проверьте `TG_TOKEN`/`TG_CHAT_ID` в переменных окружения (не в config.js!)
и что вы написали боту `/start`

---

## Контакты поддержки

- Amvera: docs.amvera.ru, Telegram @amvera_support
- ЮKassa: yookassa.ru/developers, поддержка через личный кабинет
- Яндекс Диск: yandex.ru/support/disk
